[package]
name = "tcslibgs"
version = "0.1.0"
edition = "2021"
description = "TCSpecial ground/space shared library - definitions used by both tcslib and tcspecial"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
libc = "0.2"
thiserror = "1.0"
